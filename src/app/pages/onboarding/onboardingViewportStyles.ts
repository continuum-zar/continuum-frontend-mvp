/**
 * Root shell for onboarding routes: exactly one viewport tall, no page scroll.
 * Does not change inner layout; only constrains the outer wrapper.
 */
export const onboardingRootClassName =
  "flex h-dvh max-h-dvh min-h-0 w-full flex-col items-center overflow-hidden";
